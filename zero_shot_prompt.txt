function toSnakeCase(text) {
    return text
        .replace(/[A-Z]/g, letter => `_${letter.toLowerCase()}`)
        .replace(/\s+/g, '_')
        .replace(/[^a-z0-9_]/g, '')
        .replace(/_+/g, '_')
        .replace(/^_|_$/g, '');
}

module.exports = toSnakeCase;